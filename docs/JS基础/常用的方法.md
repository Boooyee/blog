### Object转query

``` 
function serialize(obj){
  let arr = []
  for(let i in obj){
    if(obj.hasOwnProperty[i]){
      arr.push(encodeURIComponent(i) + '=' + encodeURIComponent(obj[p]))
    }
  }
  return str.join('&')
}
```
